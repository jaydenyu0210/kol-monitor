services:
  app:
    build: ./backend
    container_name: kol-monitor-backend
    restart: always
    env_file:
      - backend/.env
    ports:
      - "3001:3001"
    volumes:
      - ./backend:/app
      - /app/.credentials # Keeps browser sessions persistent
